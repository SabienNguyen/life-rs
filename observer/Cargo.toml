[package]
name = "observer"
version = "0.1.0"
edition = "2024"

[dependencies]
sim-core = { path = "../sim-core" }
person = { path = "../person" }
sim = { path = "../sim" }

[dev-dependencies]
society = { path = "../society" }
